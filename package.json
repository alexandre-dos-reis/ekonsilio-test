{
  "name": "@ek/core",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "db:generate": "bun drizzle-kit generate",
    "db:migrate": "bun drizzle-kit migrate",
    "db:push": "bun drizzle-kit push",
    "auth:generate": "bunx @better-auth/cli generate --config ./apps/backend/src/auth.ts"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "^1.0.4",
    "dotenv": "^17.2.0",
    "tsx": "^4.7.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {}
}
